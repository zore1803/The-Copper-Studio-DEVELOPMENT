node_modules/
.env
*.env
!.env.example
.DS_Store
npm-debug.log*
api.log
api.err.log
copper-crm/dist/
dist/
.local/
# Replit config holds local secrets — managed in the Replit workspace, not git.
.replit
# Local dev mongod data/logs — accidentally committed and blew the Git LFS budget, breaking Render deploys.
.mongodb/
